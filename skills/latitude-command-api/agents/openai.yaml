interface:
  display_name: "Latitude CLI"
  short_description: "Publish, deploy, and share Latitude projects through the CLI"
  default_prompt: "Use $latitude-command-api to publish, deploy, share, or manage a Latitude project via the Latitude CLI first; use direct local command API requests only if the CLI is unavailable."
